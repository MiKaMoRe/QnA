$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>')
$('.notice').html('<%= notice %>')
$('.alert').html('<%= alert %>')
<% if @answer.persisted? %>
  $('.answers').append('<%= j render @answer %>')
  $('#answer_body').val('')
  $('#answer_title').val('')
<% end %>
